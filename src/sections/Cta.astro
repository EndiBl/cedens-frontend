---
import Button from "@/components/Button.astro"
---

<section class="cta">
  <div class="cta__container">
    <h2 class="cta__title"><span class="cta__title cta__title--accent">Estás a un paso</span> de tu sonrisa perfecta</h2>
    <p class="cta__paragraph">No esperes más para cuidar de tu salud dental. Agenda tu cita ahora mismo a través de WhatsApp para una atención inmediata o déjanos tus datos y te contactaremos a la brevedad para planificar tu visita.</p>
    <div class="cta__buttons">
      <Button label="Agenda una cita" variant="secondary" />
      <Button label="Déjanos tus datos" variant="secondary" />
    </div>
  </div>
</section>

<style>
  .cta {
    background-color: var(--color-primary);
    padding: 32px;
    position: relative;
  }

  .cta__container {
    max-width: 960px;
    margin: 0 auto;
  }

  .cta__title {
    font-size: var(--text-3xl-font-size);
    line-height: var(--text-3xl-line-height);
    margin-bottom: 32px;
    font-weight: 600;
    text-align: center;
    color: var(--color-tertiary);
  }

  .cta__title--accent {
    color: var(--color-accent);
  }

  .cta__paragraph {
    font-size: var(--text-xl-font-size);
    line-height: var(--text-xl-line-height);
    color: var(--color-tertiary);
    text-align: center;
    margin-bottom: 32px;
  }

  .cta__buttons {
    display: flex;
    gap: 32px;
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (min-width: 960px) {
    .cta {
      padding: 64px;
    }

    .cta__title {
      font-size: var(--text-5xl-font-size);
      line-height: var(--text-5xl-line-height);
    }
  }
</style>